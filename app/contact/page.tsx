// types
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact | Daniel Berneisch",
};

const Contact = () => {
  return <div>contact page</div>;
};

export default Contact;
