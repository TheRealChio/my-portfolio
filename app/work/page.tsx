// components
import Projects from "@/components/projects";

// types
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Work | Daniel Berneisch",
};

const Work = () => {
  return <Projects />;
};

export default Work;
